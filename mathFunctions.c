#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main() {
  double A = sqrt(9);
  printf("square root of 9 is %lf\n", A);

  double B = pow(2, 4);
  printf("2 to the power of 4 is %lf\n", B);

  int C = round(3.14);
  printf("3.14 rounded is %d\n", C);

  int D = ceil(3.14);

  int E = floor(3.99);

  double F = fabs(-100.0);

  double G = log(3);

  double H = sin(45);

  double I = cos(45);

  double J = tan(45);

  return 0;
}
